name: flutter_dynamic_tabs
description: A package to achieve dynamic tabs that can be opened/closed, like in a browser.
version: 0.0.4
homepage: https://github.com/NamanShergill/flutter_dynamic_tabs


environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter
